/*import api from 'src/api';

import {} from 'src/actions/login';

export default (store) => (next) => (action) => {
  // console.log('Passage dans le middleware', action);
  switch (action.type) {
   
    }
    default:
      return next(action);
  }
};*/
